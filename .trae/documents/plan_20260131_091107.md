## 钱包连接设计重构计划

### 1. 移除解绑按钮
- 删除 [App.jsx:1660-1671](file:///f:/Seed-Hunter/web/src/App.jsx#L1660-L1671) 的解绑按钮代码
- 保持连接状态下的显示简洁，只显示地址和网络

### 2. 恢复圆角胶囊高度
- 移除解绑按钮后，Pill 组件的高度会自动恢复到原始形状

### 3. 添加绿色连接按钮
- 在未连接状态下，将现有的 "Connect" 按钮替换为绿色的 "连接" 按钮
- 按钮样式：`rounded-full bg-green-500/10 px-3 py-1 text-xs font-bold text-green-600 transition-colors hover:bg-green-500/15 hover:text-green-700`

### 4. 创建网络提示弹窗
- 添加新的 `walletConnectModal` 状态管理
- 创建 `WalletConnectModal` 组件，使用与其他弹窗相同的风格：
  - 遮罩层：`fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm p-4`
  - 弹窗内容：`rounded-3xl bg-white p-6 shadow-2xl ring-1 ring-black/5`

### 5. 弹窗内容
```markdown
## 请使用Kite AI网络进行体验

## 连接方法：
1. 打开MetaMask钱包，选择网络，选择自定义网络
2.输入以下内容：
网络名称: KiteAI Testnet
RPC URL: `https://rpc-testnet.gokite.ai`
Chain ID: 2368
浏览器: `https://testnet.kitescan.ai`
```

### 6. 交互逻辑
- 点击绿色 "连接" 按钮 → 显示 `WalletConnectModal` 弹窗
- 点击弹窗中的 "连接" 按钮 → 调用 `wallet.connect()` 方法
- 连接成功后 → 关闭弹窗，返回原始界面
- 连接失败或取消 → 关闭弹窗

### 7. 代码修改点
1. **App.jsx:1644-1681** - 修改钱包连接 UI
2. **App.jsx** - 添加 `walletConnectModal` 状态
3. **App.jsx** - 添加 `WalletConnectModal` 组件
4. **App.jsx** - 添加弹窗渲染